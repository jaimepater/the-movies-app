import styled from 'styled-components';
import { Container as MuiContainer } from '@material-ui/core';
import { StaticSpacing } from '../../theme/theme';

export const Page = styled(MuiContainer)`
  margin-top: ${StaticSpacing.MD};
`;
